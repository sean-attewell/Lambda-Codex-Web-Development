// The internet is a network of interconnected computers
// The web is just one way to share data over the internet. 
// It consists of a body of information stored on web servers, ready to be shared worldwide.

// A web server stores the code for different websites to be shared across the world at all times. 
// When we load the code for our websites, or web apps, on a server like this, we say that the server is "hosting" our website/app.

// However, the server itself needs some code to tell it what to do. 
// That software program is called - a web server!
// Its main purpose is to "serve" web pages it retrieves from your project code to users upon request.

// While hosting websites has traditionally been a fairly involved task, nowadays, it is quicker than it ever has been before. 
// Gone are the days of setting up a server program through services like Apache or Windows IIS, 
// renting the physical server space from a large company, or buying server hardware.


// Domain Names and URLs
// In 1984, a couple of decades after the first computers and networks were invented, the Domain Name Service (DNS) was introduced. 
// This was a concept where computers could distinguish themselves from one another. 
// As a result, six domains that you will probably recognize were introduced: edu (education), gov (government), mil (military), com (commercial), net (network resources), and org (organization).

// Just a few short years later, in 1991, the worldwide web network was opened to the world and incorporated the new HTML computer language. 
// HTML uses specifications for Uniform Resource Locators (URLs). 
// These URLs became (and remain) the universal standard for locating website addresses.

// By definition, a URL is a formatted text string referring to the location of a resource on a computer network (most commonly the web). 
// Typically, these resources are web pages, but they can also be text documents, graphics, programs, or pretty much anything that can be stored digitally.

// Combining this all together, we learn that computers can connect to the internet via the world wide web, locate a specific server computer through a URL and domain name, and retrieve information or resources from that server.


// https://www.doteasy.com/domain-email-and-website-hosting-articles/how-do-websites-work

// A website is simply a collection of web pages of codes – codes that describes the layout, format and content on a page. 
// The web server is a internet-connected computer that receives the request for a web page sent by your browser.
// The browser connects your computer to the server through an IP address. The IP address is obtained by translating the domain name.

// In other words, in order to display your website on the Internet, you will need:
// A website
// A domain name
// A server 




// Many services allow you to "rent" server space for your web app. You can deploy web apps to Amazon AWS servers, Google Servers, Netlify, Heroku, Gatsby, Github Pages, and Vercel (which we'll learn about in this module) among many, many others.

// Services like these are a huge step up in modern technology that allows us to deploy sites with the click of a button or right in your terminal.

// Deploying a static web app is a little different than deploying a dynamic web app.
// A basic definition of a static site has hardcoded data that doesn't change. 
// A lot of our React apps use data from third-party libraries but still deploy like a static app.

// Deploying a site with an accompanying server and database will be more complicated than just an almost static site that consumes a third-party API. 
// We'll learn more about that kind of deployment in the backend unit.


// https://vercel.com/#get-started

// Service which which you can import github
// Or start with a template like create react app, and had pushed changes be live immediately.

